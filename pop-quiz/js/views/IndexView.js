define(["Backbone",
        "Handlebars",
        "../config", ,
        ],
    function (Backbone,
        Handlebars,
        config
    ) {
        var IndexView = Backbone.View.extend({});

        return IndexView;
    });